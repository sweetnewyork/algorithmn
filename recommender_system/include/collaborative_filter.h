#ifndef COLLABORATIVE_FILTER_H
#define COLLABORATIVE_FILTER_H

#include "common.h"

//2.compute the socres of the users and then the score of
//+ the items
extern void collaborative_filter(user_container*, item_container*);

#endif